$schema: "https://moonrepo.dev/schemas/project.json"

language: "typescript"
stack: "backend"

tasks:
  dev:
    command: "tsx src/index.ts"
    options:
      cache: false
  wa:
    command: "tsx src/index-gateway.ts"
    options:
      cache: false
