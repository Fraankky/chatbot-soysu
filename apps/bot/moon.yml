$schema: "https://moonrepo.dev/schemas/project.json"

language: "typescript"
stack: "backend"

tasks:
  dev:
    command: "pnpm dev"
    options:
      cache: false
  wa:
    command: "pnpm wa"
    options:
      cache: false
