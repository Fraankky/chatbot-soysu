$schema: "https://moonrepo.dev/schemas/project.json"

language: "typescript"
stack: "frontend"

tasks:
  dev:
    command: "pnpm dev"
    options:
      cache: false
  build:
    command: "pnpm build"
